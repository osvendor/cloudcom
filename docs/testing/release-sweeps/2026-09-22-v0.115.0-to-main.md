# Pre-release sweep — v0.115.0 → main (2026-09-22)

| | |
|---|---|
| Range | `v0.115.0` → `origin/main` `70868c3890` (98 PRs) |
| Gate | Enqueued + merged before sweep: #6662 #6661 #6655 #6646 #6642 #6643. Held (no review-pr on final head / CI red / conflicting): #6630 #5855 #5489 #6610 #6574 #6538 #5856; draft #6644. |
| Stack | `pnpm wt-stack up` on `qa/sweep-post-v0.115.0` @ `70868c3890`, baseUrl `http://localhost:32793` |
| Flags enabled | admin promoted to platform admin; `WEBAUTHN_RP_ID=localhost`, `MFA_FORCE_FOR_PARTNER_ADMIN=false`; topology via Settings ▸ Partner ▸ Modules (in-sweep) |
| Driver | skill `pre-release-sweep`; inventory agent (sonnet); browser sweep agents (opus), one group at a time |

## Change inventory

Status ∈ `TODO | PASS | PARTIAL | FAIL | BLOCKED | N/A`. `N/A` up front = no UI surface (deps/docs/test/CI, api-/agent-only); covered by CI only.

| PR | Title | Surface | Route / click-path | Expected visible outcome | Prereq | Group | Status |
|---|---|---|---|---|---|---|---|
| #6646 | Portal Network overview page + nav entry | portal | Portal (`/portal`) ▸ sidebar "Network" ▸ Network overview | Page lists the org's network assets; nav entry present | Portal user; `enableNetworkVisibility` portal flag on | Portal | PASS |
| #6661 | GFS retention: retentionDays is a floor keepDaily can't shorten | web | Backup ▸ Backup configs/profiles ▸ edit retention (GFS keepDaily < retentionDays) | Retention UI/API honours retentionDays as a floor (warning or clamped) | None | Devices, backup & topology | PASS |
| #6642 | Winget sync holds no tx across GitHub; wedge scanner reclaims | api-only | N/A | No idle-in-tx wedge during winget sync | Worker + GitHub | N/A-noUI | N/A |
| #6643 | Gate system cleanup on agent 0.116.0 | web-observable | Devices ▸ [device] ▸ Filesystem ▸ System cleanup on a 0.115.x agent | "Update the agent" message (409 agent_update_required) | Device with agentVersion 0.115.x (SQL) | Devices, backup & topology | PASS |
| #6662 | Deterministic heartbeat watchdog/scheduleCleanup tests | agent-only (test) | N/A | N/A | N/A | N/A-noUI | N/A |
| #6655 | docs: metric anomaly episodes spec + plans | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6491 | Bare-metal agent refuses provision on unresolved cross-snapshot refs | agent-only | N/A (agent CLI: `breeze-backup rebuild`) | Rebuild refuses with an error before provisioning when a cross-snapshot file reference can't be resolved | Live agent + a `snapshot-file-membership-v1` multi-snapshot backup set | N/A-noUI | N/A |
| #6657 | Optional customer notice on provider-recorded acceptance | web | Settings ▸ Billing (`/settings/billing`) ▸ Partner Billing Settings ▸ Defaults ▸ "Email the invoice automatically when a quote is accepted"; also shown in Billing ▸ Quotes ▸ quote detail ▸ "Accept on behalf" dialog | Toggling the default changes whether accept-on-behalf auto-emails the new invoice | Partner billing-admin role | Billing & quotes | PASS |
| #6663 | Attach evidence file to accept-on-behalf record | web | Billing ▸ Quotes ▸ `/billing/quotes/[id]` ▸ QuoteActions menu ▸ "Accept on behalf" ▸ Evidence upload field | Uploaded file appears attached to the acceptance record after submit | `quotes:accept` permission; quote in a sendable state | Billing & quotes | PASS |
| #6659 | Decline a quote on behalf of a customer | web | Same quote detail ▸ QuoteActions menu ▸ "Decline on behalf" ▸ dialog "Record the customer's decline" ▸ optional reason ▸ "Mark declined" | Quote status flips to Declined | `quotes:accept`-equivalent permission | Billing & quotes | PASS |
| #6660 | Friendly copy for send-gate refusals in accept-on-behalf dialog | web | Accept-on-behalf dialog, trigger a send-gate refusal (e.g. unresolved contract variables) | Human-readable error text instead of a raw error code | A quote with unresolved contract variables | Billing & quotes | PASS |
| #6656 | Accept-on-behalf dialog reads auto-email flag from quote detail | web | Accept-on-behalf dialog checkbox | Email toggle pre-fills correctly from partner default without a race | None | Billing & quotes | PASS |
| #6658 | Write accept-on-behalf audit before post-commit side effects | api-only | N/A | Audit-log row always present even if a downstream side effect fails | None | N/A-noUI | N/A |
| #6639 | Retired-pricing rejection deferred to v0.116, not v0.115 | api-only (guard/doc) | N/A | Legacy labour-pricing fields still accepted in v0.115; rejection lands next release | None | N/A-noUI | N/A |
| #6647 | pwsh cold-start headroom in agent test | agent-only (test) | N/A | CI test stability only | N/A | N/A-noUI | N/A |
| #6536 | chore(deps): docker-base-images bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6596 | Accept a quote on behalf of a customer, issue invoice (flagship) | web | Billing ▸ Quotes ▸ quote detail ▸ QuoteActions ▸ "Accept on behalf" ▸ fill method/signer ▸ "Accept and issue invoice" | Quote → Accepted, invoice number shown with link to new invoice | New `quotes:accept` permission (back-filled to existing "can send" roles) | Billing & quotes | PASS |
| #6628 | docs(deploy): prune stale images after droplet deploy | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6549 | docs(ai): Tool Catalog W02 plan | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6608 | chore(deps): batch dependabot bumps | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6623 | Emit INVALID_CREDENTIALS from genericAuthError for SSO login | web/api | Sign-in page, fail an SSO login | "Invalid credentials" error instead of a generic 500 | Live SSO/IdP configured | Settings & admin | PASS |
| #6529 | chore(deps): resend bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6624 | chore(deps): go-minor-patch group in /agent | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6540 | Partner Modules toggle for Network Topology + device-tab empty state | web | Settings ▸ Partner (`/settings/partner#modules`) ▸ Modules card ▸ "Enable network topology" (+ Physical/Interface Health/Diagnostics sub-toggles); Devices ▸ [device] ▸ Topology tab shows Off/Preparing/Unavailable empty state with "Open Modules" link | Flipping the flag changes the device Topology tab's empty-state copy and unlocks the tab | Partner-admin role; topology is beta/off by default (`settings.topologyFeatureFlags`) | Devices, backup & topology | PASS |
| #6553 | Mailbox Connect 500 + Microsoft admin-consent redirect handled | web/api | Settings ▸ Org ▸ Ticketing ▸ Inbound Email card ▸ "Connect" (Microsoft mailbox) | On admin-consent denial, friendly error banner instead of a 500 | Live M365 app registration / Microsoft admin-consent flow | Ticketing & mailbox | PASS |
| #6576 | Caller-verification backend core, flag off (W01) | api-only, flag off | N/A | Authenticated `/caller-verification` routes 404 `feature_disabled` | `CALLER_VERIFICATION_ENABLED=true` (must be exact literal `true`) | N/A-noUI | N/A |
| #6573 | Live IOC scan scheduler, Security tab cleanup, Scans page, quarantine | web | Sidebar ▸ Security ▸ "IOC Scans" (`/security/scans`); Configuration Policies ▸ Security tab | Scans page loads with scan/threat list; Security tab shows cleaned-up config UI | `devices:read`; live agent scan data for non-empty results | Monitoring, security & AI | PASS |
| #6600 | Human-work steps bound to ticket checklists + maintenance-window wait writer (E3) | web/api | Ticket ▸ Checklist card (`TicketChecklistCard`) shows a step sourced from an AI Operator task | Checklist item appears/completes as the operator task progresses | AI Operator recipe with a human-work step actually running | Monitoring, security & AI | PARTIAL |
| #6602 | System-cleanup paper cuts (W05 lab run) | web/api | Devices ▸ [device] ▸ Filesystem tab ▸ "Cleanup Run History" panel | Cleanup run history renders correctly post-fix | Live agent with an executed disk-cleanup run | Devices, backup & topology | PASS |
| #6606 | Retired cleanmgr sub-id reachable from selectionFor | agent-only | N/A | Windows disk-cleanup catalog selection no longer skips retired sub-id | Live Windows agent | N/A-noUI | N/A |
| #6609 | chore(deps): typescript-tooling + perf_hooks fix | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6616 | billable_minutes CHECK violation → typed refusal | web | Ticket ▸ Time & Billing tab ▸ add an invalid time entry ▸ Save | Friendly refusal message instead of a 500 | None | Billing & quotes | BLOCKED |
| #6625 | Playwright worker logs in once, owns its refresh-token family | docs-chore (test infra) | N/A | N/A | N/A | N/A-noUI | N/A |
| #6619 | Wait for fire-and-forget move-org audit rows instead of sleeping | docs-chore (test) | N/A | N/A | N/A | N/A-noUI | N/A |
| #6618 | Persist agent's verification error as the failure reason | web | Devices ▸ [device] ▸ Backup ▸ Verification tab | Failed verification row shows the real agent error text | Backup verification configured + a failed run | Devices, backup & topology | PARTIAL |
| #6617 | Only re-derive billed minutes when terms actually change | web-observable | Ticket ▸ Time & Billing ▸ edit unrelated billing field | Billed minutes stay stable across unrelated saves | None | Billing & quotes | PASS |
| #6615 | Restore monitor preview auth via canonical closures | web | Configuration Policies ▸ Monitors tab ▸ "Preview" a conversion | Preview succeeds instead of an auth error | None | Monitoring, security & AI | PARTIAL |
| #6614 | Guard agentWs onOpen markOnline await | api-only | N/A | Prevents a crash on device reconnect race | Live agent reconnect | N/A-noUI | N/A |
| #6613 | Refuse zero-row deleteTicketPart instead of reporting success | web | Ticket ▸ Time & Billing ▸ delete an already-deleted part (stale UI) | Error shown instead of a false success | Stale client state | Billing & quotes | PARTIAL |
| #6612 | Round invoice delete preview at currency minor unit | web | Billing ▸ Invoices ▸ [invoice] ▸ InvoiceEditor ▸ delete a line | Delete-preview total rounds correctly | None | Billing & quotes | PASS |
| #6611 | Stand down rate limiters redding portal dev E2E | docs-chore (ci) | N/A | N/A | N/A | N/A-noUI | N/A |
| #6519 | chore(deps): astro group bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6591 | Backup-providers W02 sync — job, persistence, mapping, alerts | api-only | N/A | Cove backup stats sync into Breeze's device/alert model | Live Cove backup-provider account + BullMQ worker | N/A-noUI | N/A |
| #6547 | End cleanmgr's session-0 hang | agent-only | N/A | Disk cleanup no longer hangs / mis-reports completion | Live Windows agent | N/A-noUI | N/A |
| #6520 | chore(deps): react group bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6521 | chore(deps): @tanstack/react-query bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6580 | DR plan save and BMR token create surface via runAction | web | Disaster Recovery (`/dr`) ▸ plan editor ▸ "Save"; Devices ▸ [device] ▸ Backup ▸ Recovery tab ▸ "Create BMR token" | Both actions now show a success/error toast instead of failing silently | None | Devices, backup & topology | PASS |
| #6522 | chore(deps): autoprefixer bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6579 | Require a click to confirm email verification | web | `/auth/verify-email?token=...` | Shows "Confirm your email" with a "Confirm my email" button instead of auto-verifying on page load | A pending verification email token | Settings & admin | PASS |
| #6175 | Customer Portal Network Visibility read model + flag (PR 1/2) | portal, flag off | Settings ▸ Org ▸ Portal settings (OrgPortalSettingsEditor) ▸ "Network Visibility" toggle | Flag persists; portal-facing UI ships in a later PR | `enableNetworkVisibility` portal flag off by default; UI not yet wired | Portal | PASS |
| #6582 | docs(reports): PSA business reports spec + W01–W03 plans | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6595 | AI Operator task targets/accounts/steps/events (recipe library E2) | api-only | N/A | Backend graph consumed by #6600's checklist UI | AI Operator running | N/A-noUI | N/A |
| #6523 | chore(deps): hono bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6578 | Scroll validation errors into view on Backup/DR forms | web | Devices ▸ [device] ▸ Backup tab, or `/dr` plan editor ▸ submit an invalid form | Page auto-scrolls to the first invalid field | None | Devices, backup & topology | PARTIAL |
| #6566 | Persist offline transitions from WS close/error handlers | web-observable | Devices (`/devices`) list | Device correctly shows "Offline" after agent disconnect (no longer stuck Online) | Live agent that disconnects | Devices, backup & topology | BLOCKED |
| #6587 | Stop baking worked-vs-billed note into invoice descriptions | web/portal | Billing ▸ Invoices ▸ [invoice] (InvoiceDetail/InvoiceDocument); Portal ▸ Invoices ▸ [invoice] (InvoiceDetailView) | Line descriptions no longer contain a baked-in "(billed X of Y worked)" note | None | Billing & quotes | PASS |
| #6588 | Refuse zero-row updateTicketPart write | web | Ticket ▸ Time & Billing ▸ edit a stale time/part entry | Error instead of echoing the stale row as success | Stale client state | Billing & quotes | PARTIAL |
| #6583 | listBillables and invoiceAssembly agree on missing-rate gap semantics | web-observable | Ticket ▸ Time & Billing / Billing ▸ Invoice assembly | Consistent "no rate" handling between the billables list and the assembled invoice | A ticket with an unrated work type | Billing & quotes | PASS |
| #6581 | Show a no-rate indicator instead of inflated billable hours | web | Ticket ▸ Time & Billing tab (TicketTimeBilling) | "No hourly rate — billable time without a rate cannot be invoiced" badge on the entry | A time entry with no hourly rate configured | Billing & quotes | PASS |
| #6524 | chore(deps): @testing-library/dom bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6565 | Never echo WebAuthn expected origin on passkey verification failure | web-observable | Settings ▸ Security ▸ Passkeys, or a failed passkey login | Failure error no longer leaks the expected origin string | A failed passkey verification attempt | Settings & admin | PASS |
| #6569 | Canonicalize S3 accessKeyId/secretAccessKey to accessKey/secretKey | web | Devices/Org ▸ Backup ▸ Backup Config ▸ add S3-compatible destination | Credential field names normalized on save | External S3-compatible backup provider credentials | Devices, backup & topology | PASS |
| #6556 | DELETE /monitor-definitions/:id succeeds once alerts exist | web | Configuration Policies ▸ Monitors tab ▸ delete a monitor with fired alerts | Deletion succeeds instead of an FK error | A monitor with existing alert history | Monitoring, security & AI | PASS |
| #6560 | Stop /devices/network from rendering a phantom device page | web | Navigate directly to `/devices/network` | Renders the network/topology list view, not a broken device-detail shell | None | Devices, backup & topology | PASS |
| #6557 | Show the reason for a plain server-refused AI tool call | web | Workspace (`/workspace`) AI Assistant chat, trigger a server-refused tool call | AiToolCallCard shows the refusal reason text | `ai_sessions:use` permission; AI enabled | Monitoring, security & AI | PASS |
| #6546 | Settle free-space reading before computing freedBytes (lazy-reclaim FS) | agent-only | N/A | Correct "space freed" numbers on btrfs/ZFS-like filesystems | Live agent on a lazy-reclaim filesystem | N/A-noUI | N/A |
| #6525 | chore(deps): eslint bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #5570 | SEC-050/141/144/145: tier-2 release authority, admin history bounds, contract-tool/line-materialization caller authority | api-only (security hardening) | N/A | Unauthorized AI tool/contract calls now denied at the authority boundary | An AI session attempting an over-privileged admin/contract action | N/A-noUI | N/A |
| #6552 | Backup provider foundation — schema, Cove adapter, connection routes | api-only | N/A | New `/backup/providers` routes for connecting Cove | Live Cove backup-provider account | N/A-noUI | N/A |
| #6567 | Refuse zero-row updateTimeEntry write | web | Ticket ▸ Time & Billing ▸ edit a stale time entry | Error instead of echoing the stale row | Stale client state | Billing & quotes | PARTIAL |
| #6563 | Guard persistDrAuthorizationDenial with compare-and-swap | api-only | N/A | Prevents a race double-writing a DR authorization denial | Concurrent DR authorization requests | N/A-noUI | N/A |
| #6564 | Stop resurrecting deleted monitors on the Monitors tab | web | Configuration Policies ▸ [policy] ▸ Monitors tab ▸ delete a monitor ▸ save ▸ reopen | Deleted monitor no longer reappears | None | Monitoring, security & AI | PASS |
| #6562 | Apply configured maxTurnsPerSession to new AI sessions | web-observable | Workspace AI Assistant chat | Session ends once the admin-configured turn limit is hit | Admin sets a turn limit in Settings ▸ AI Usage/AI Agents | Monitoring, security & AI | PASS |
| #6559 | Gate /workspace and AI Assistant nav on ai_sessions:use | web | Sidebar — "AI Assistant" nav item; direct nav to `/workspace` | Nav item hidden and route blocked without the permission | Role lacking `ai_sessions:use` | Monitoring, security & AI | PASS |
| #6558 | Default followRedirects off for a 3xx http_check expectation | web | Configuration Policies ▸ Monitors tab ▸ create an HTTP check expecting a 3xx status | "Follow redirects" defaults off | None | Monitoring, security & AI | PASS |
| #6533 | Complete Microsoft consent callback flow | web | Settings ▸ Integrations ▸ M365 (M365CustomerGraphReadCard) ▸ "Connect" | Connection status flips to Active after the Microsoft consent popup returns | Live M365 tenant + app registration | Settings & admin | PARTIAL |
| #6551 | AI Operator recipe registry + coordinator dispatch refactor (E1) | api-only | N/A | Internal refactor; no direct UI change | AI Operator running | N/A-noUI | N/A |
| #6548 | Refuse bare_metal BMR token mint when snapshot isn't restorable | web | Devices ▸ [device] ▸ Backup ▸ Recovery tab ▸ "Create BMR token" on a non-restorable snapshot | Refused with an error instead of minting a broken token | A backup snapshot marked non-restorable | Devices, backup & topology | PASS |
| #6545 | Dedupe btrfs bind mounts by device in cleanup volume scan | agent-only | N/A | Correct free-space totals on btrfs hosts with bind mounts | Live agent on btrfs | N/A-noUI | N/A |
| #6542 | Settings consolidation W02 — shared InheritedField, SLA notes, InboundEmailCard split | web | Settings ▸ Org ▸ Ticketing (OrgTicketSettingsEditor) / Billing (OrgBillingSettings) — fields show an "Inherited from partner default" indicator; Inbound Email card split out | Inherited-field badges appear; Inbound Email card has its own enable/connect controls | None | Settings & admin | PASS |
| #6468 | AI tools description diet — one description surface, 300/160 budget | api-only | N/A | Smaller AI tool-call payloads / prompt budget | An AI session using the tool catalog | N/A-noUI | N/A |
| #6480 | Monitor conversion pre-transaction reads take caller's DB context (regression from #6416) | web-observable | Configuration Policies ▸ Monitors tab ▸ convert a legacy monitor | Conversion succeeds without cross-tenant DB context bug | None | Monitoring, security & AI | PASS |
| #6554 | Wait for fire-and-forget move-org audit instead of racing it | docs-chore (test) | N/A | N/A | N/A | N/A-noUI | N/A |
| #6550 | Deflake TestRunBrewCleanupBoundedCancelsDescendants under -race | docs-chore (test) | N/A | N/A | N/A | N/A-noUI | N/A |
| #6544 | docs(security): caller-verification (anti-vishing) spec v5 + W01–W05 plans; service-deliverables plans | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6543 | docs(security): YARA IOC scanning W01 live-policy plan + index | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6527 | chore(deps): @elastic/elasticsearch bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6532 | Check IP allowlist before request transaction | web-observable | Login from an IP outside partner's Settings ▸ Security ▸ IP Allowlist | Blocked before a DB transaction opens (perf/security only, same denial UX) | Partner has an IP allowlist configured | Settings & admin | PASS |
| #6235 | Role and OS filters on partner-owned assignments | web | Configuration Policies ▸ [policy] ▸ Assignments tab ▸ edit assignment ▸ "Role"/"OS" filter selectors (AssignmentRoleOsFilters) | Filters narrow which devices/roles the assignment applies to | Partner-owned policy assignment | Settings & admin | PASS |
| #6471 | Scope mobile-native detection to merge-queue entry's own base | docs-chore (ci) | N/A | N/A | N/A | N/A-noUI | N/A |
| #6530 | BullMQ-safe SentinelOne sync job IDs | api-only | N/A | Prevents job-ID collisions in the SentinelOne sync queue | SentinelOne integration configured | N/A-noUI | N/A |
| #6531 | BullMQ-safe config-policy run IDs | api-only | N/A | Prevents job-ID collisions in policy-run queue | None | N/A-noUI | N/A |
| #6534 | Harden inbound email-to-ticket: flood backpressure, loop/bounce suppression, opt-in full-message replies | web/api | Settings ▸ Org ▸ Ticketing ▸ Inbound Email card ▸ "opt-in full-message replies" toggle | Inbound emails processed under a global rate ceiling with loop/bounce suppression | Live inbound-email domain (e.g. Mailgun) | Ticketing & mailbox | PARTIAL |
| #6478 | docs(ai): A-W05 output-efficiency wave plan + index decisions D10–D16 | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6477 | Reject retired labour-pricing fields instead of silently dropping them | web-observable | Settings ▸ Org ▸ Ticket settings — attempt to save a retired labour-pricing field | Request rejected with a clear error instead of silently dropped | None | Billing & quotes | PASS |
| #6479 | Stop advertising tenant tier-3 tools that tools/call always denies | api-only | N/A | Tenant-scoped MCP clients see a shorter, accurate tool list | An MCP client connected with a tenant-tier API key | N/A-noUI | N/A |
| #6528 | chore(deps): zustand bump | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |
| #6513 | docs: v0.115.0 release docs sweep | docs-chore | N/A | N/A | N/A | N/A-noUI | N/A |

## Sweep log (append as you go)

### Billing & quotes — 2026-09-23 00:10Z

#### #6596 — PASS
✅ Seeded draft quote Q-2026-0001 ("QA accept", 1 manual line 2 × $150) via API. `/billing/quotes/<id>` ▸ "Accept on behalf" (draft) ▸ dialog "Record the customer's acceptance" listed consequences ("An invoice for $300.00 will be issued and numbered now", "The invoice will be emailed…", draft warning). Filled Reference PO-QA-1001 / Who agreed "Jane Customer" ▸ "Accept and issue invoice" → toast "Quote accepted. Invoice INV-2026-0001 issued."; status → Converted; banner "Accepted on behalf of Jane Customer by Breeze Admin on 9/22/2026 · Verbally / by phone · PO-QA-1001" + "View invoice →" link to `/billing/invoices/c166bc0b…`. API POST accept-on-behalf 200.
⚠️ Quote Details shows "Issued 9/23/2026" next to "Created 9/22/2026 / Accepted 9/22/2026" for the same moment (00:08Z) — issued date rendered in UTC, the others in local time.

#### #6663 — PASS
✅ Same accept: attached `po-evidence.pdf` in "Evidence file (optional)" → second toast "Evidence attached."; quote page shows "Download evidence — po-evidence.pdf" and "Replace evidence".

#### #6657 — PASS
✅ Settings ▸ Billing ▸ **Documents** tab (not Defaults, as the inventory says) ▸ checkbox "Email the customer when you record an acceptance on their behalf" ticked ▸ "Save billing settings" → toast "Billing settings saved"; `partners.notify_customer_on_behalf_acceptance = t`. With it on, the accept above fired `[quoteOnBehalfNotify]` to the org billing contact (Resend rejected the example.com address — stack's live key, expected); with it off (and auto-email off) a second accept (INV-2026-0002) logged no notice/auto-email attempt.
⚠️ When the auto-email / customer notice fails to send (logged `auto-email failed (accept already committed)`), the tech gets only the success toast — no hint the customer was never emailed.

#### #6656 — PASS
✅ Partner auto-email ON → dialog shows "The invoice will be emailed to the customer." (`accept-on-behalf-consequence-email`). Flag set OFF → fresh page load, dialog omits the line. Value comes from the quote detail payload (`autoEmailInvoiceOnAccept` key present in GET /quotes/:id); no separate fetch.

#### #6659 — PASS
✅ Quote Q-2026-0002 set to `sent` via SQL (Send needs deliverable email). "Decline on behalf" ▸ "Record the customer's decline" ▸ Reference + reason "Budget freeze" ▸ "Mark declined" → toast "Quote marked declined."; status Declined; banner "Declined 9/22/2026 · Customer's note: “Budget freeze”" + "Duplicate & revise"; Accept/Decline buttons gone.
⚠️ The declined banner doesn't say it was recorded on the customer's behalf (who/method/reference), unlike the accepted banner — and labels the tech-typed reason as "Customer's note".

#### #6660 — PASS
✅ Draft Q-2026-0003 with `deposit_type='selected_lines'` and no eligible line (SQL) ▸ Accept on behalf ▸ submit → API 409 DEPOSIT_INVALID; toast "This quote's deposit can't be collected as configured. Close this dialog, fix the deposit settings below, then try again." (no raw code); dialog stays open. Contract-variable branch not exercised (needs a published contract template) — covered by `AcceptOnBehalfDialog.friendlyErrors.test.tsx`.

#### #6477 — PASS
✅ No web surface sends the retired fields any more (grep of apps/web: none), so verified at the API with the browser session. `PATCH /orgs/organizations/:id/ticket-settings {defaultHourlyRate:150}` → **400** "defaultHourlyRate was retired in v0.116: labour rates and billability now come from the organization's billing profile. Assign one with PUT /organizations/:id/billing-profile, or edit rates under Settings → Billing → Rates"; same for `{rateCurrency:'EUR'}` → 400; `POST /ticket-categories {defaultBillable:true}` → 400 "defaultBillable was retired in v0.116: a category no longer carries pricing. Set defaultWorkTypeId…". GET ticket-settings still 200.
⚠️ Top-level `error` repeats the field name ("defaultHourlyRate: defaultHourlyRate was retired…"). Message says v0.116 while the PR body said v0.115 — correct for the next cut, just noting.

#### #6581 — PASS
✅ Ticket T-2026-0001 (created via API; default billing profile "Standard rates" has no base rate). Time & Billing ▸ "Log time" ▸ 30 min, no rate → form shows "Billable · rate not configured" and "No hourly rate — billable time without a rate cannot be invoiced."; Save → toast "Time logged"; summary Billable 30m + "1 entry has no hourly rate — not counted in the amount above", Time amount "—". Adding a 60 min entry at $120 → Billable 1h 30m, amount $120.00, missing-rate note still 1.
⚠️ The missing-rate note says "not counted in the amount **above**" but renders *above* the "Time amount" row (between Billable and Time amount).

#### #6583 — PASS
✅ Same ticket: billables CSV (`GET /tickets/export/billables.csv`, the Invoices ▸ Billables export card's request) → rated row `120.00`, no-rate row amount `MISSING_RATE` with blank rate (not a $0.00 line). Ticket ▸ "Create invoice" → toast "Draft invoice created" + panel "A draft invoice was created, but some billable work … was left out … Time entries with no hourly rate (not invoiced…): No-rate work — 0.50 h"; draft invoice total $120.00. Both surfaces treat the null rate as a gap.

#### #6617 — PASS
✅ Simulated a pre-feature row via SQL (T-2026-0003 entry: 20 min worked, `billable_minutes` NULL, `rounding_increment_minutes` back-stamped 15). Timesheet ▸ Edit ▸ changed rate $100→$110 + description ▸ Save → toast "Entry updated"; DB after: `billable_minutes` still NULL, rounding 15 — not re-derived to 30 by a rate-only PATCH.
⚠️ Timesheet row shows the rate twice — "$110.00/h" in the outcome badge and a bare "$110.00" in the rate column, which reads as the entry's amount (20 min would be $36.67).

#### #6567 — PARTIAL
✅ Stale UI: Timesheet ▸ Edit on an entry, entry deleted via API behind it, then Save → PATCH **404** `{"error":"Time entry not found","code":"ENTRY_NOT_FOUND"}` and error toast "Time entry not found" (no false success). The fix's actual path — a zero-row UPDATE between the lock-read and the write (org move / RLS change mid-request) — can't be staged from the browser; covered by `timeEntryService.test.ts`.
⚠️ After the 404 the timesheet stays in edit mode and keeps listing the deleted entry; no reload.

#### #6588 — PARTIAL
✅ Ticket T-2026-0003 with parts "QA cable A/B" (API-seeded, page reloaded). Parts ▸ Edit on A, A deleted via API behind the open form, Update → PATCH `/tickets/parts/:id` **404** `{"error":"Part not found"}`, toast "Part not found" — no false success. The zero-row UPDATE race itself (row re-pointed between lock-read and write) isn't stageable from the browser; covered by `timeEntryService.test.ts`.

#### #6613 — PARTIAL
✅ Same ticket: B deleted via API, then stale UI Delete ▸ Confirm → DELETE `/tickets/parts/:id` **404** `{"error":"Part not found"}`, toast "Part not found" (not "deleted"). Zero-row DELETE race path is unit-tested only (`parts.test.ts`, `timeEntryService.test.ts`).
⚠️ After either 404 the parts card keeps showing the gone part (and its Delete?/Confirm row, or the edit form) — no refetch; the user has to reload to see reality.

#### #6612 — PASS
✅ Manual invoice switched to **JPY** (`POST /invoices/:id/currency`), org tax 8.25%, lines ¥1,001/¥1,003/¥1,005/¥7/¥3 all taxable → rail Subtotal ¥3,019 · Tax ¥249 · Total ¥3,268. Editor ▸ Remove "Line D" (¥7) → during the undo window (0.3 s–4 s, toast "Line deleted · Undo") the preview read ¥3,012 · ¥248 · ¥3,260; after the flush + refetch (8 s, 12 s) identical, and DB `subtotal 3012.00 / tax_total 248.00 / total 3260.00`. No yen drift between optimistic and server totals.

#### #6587 — PASS
✅ Ticket T-2026-0002: 15 min worked at $100/h under a 60 min profile minimum (ticket shows "0.25 h worked · 1.00 h billed") ▸ Create invoice → toast "Draft invoice created". DB `invoice_lines.description = 'Short call'`, `worked_minutes = 15` — no baked note. Web Editor, Preview and Detail tabs each render the note separately ("0.25 h worked · 1.00 h billed") under the clean description. Issued (INV-2026-0003) and opened **portal** `/portal/invoices/:id` as portal@breeze.local → line "Short call" + "0.25 h worked · 1.00 h billed" on its own line, description clean.

#### #6616 — BLOCKED
BLOCKED: the refusal only fires when the TypeScript billable-minutes computation and the `time_entries_billable_minutes_chk` SQL formula disagree (23514) — no browser input produces that drift, and forcing it means altering the CHECK on a shared stack. Verified statically only: `refuseBillableMinutesDrift` (`apps/api/src/services/timeEntryService.ts:150`) wraps create/stop/update and logs `BILLABLE_MINUTES_DRIFT`; covered by `timeEntryService.test.ts`. Normal quick-add (30 min, 60 min) and a 15 min entry under a 60 min minimum all saved with `billable_minutes` matching the CHECK.

<!-- end billing & quotes -->

### Devices, backup & topology — 2026-09-23 00:20Z

#### #6560 — PASS
✅ Direct nav to `/devices/network` → redirected to `/devices#deviceClass=network`; main renders "Devices & Assets" list with the Network segment selected ("Network 0", "0 of 0 devices") — no phantom device-detail shell / Wake / Run Script buttons.

#### #6643 — PASS
✅ Device e2e-windows.local, `agent_version='0.115.0'` (SQL) ▸ Filesystem ▸ System cleanup ▸ "Check available actions" → POST `/filesystem/system-cleanup/list` 409 `agent_update_required`; amber banner (`system-cleanup-agent-update`) "Update this device's agent to version 0.116.0 or later to run OS cleanup actions." and the Check/Run buttons disabled. Set `agent_version='0.116.0'`, reload ▸ Check → **202** Accepted, no banner, button "Checking…" and polling `GET …/list/<commandId>` (device is offline, so the command just waits).
⚠️ On the 409 the same sentence is shown twice — as an error toast and as the inline banner.

#### #6602 — PASS
✅ F-6: with e2e-windows.local ▸ Filesystem open, inserted a `kind='system', status='running'` run via SQL (no local action) → row `cleanup-run-768d2209…` appeared in "Cleanup history" after ~14 s without reload ("System cleanup · 0 B reclaimed · Running"). F-5: `POST …/system-cleanup/run/768d2209…/cancel` (browser session token) → **200** `{"success":true,"data":{"status":"failed"}}`; repeat → **409** `{"error":"not_running","status":"failed"}`; next 20 s poll re-rendered the row as "Failed" with "cancelled_by_operator". F-4 (AI tool copy) / F-7 (dev tmpfs) not browser-observable. Executed-by-agent run not exercised (no live agent).
⚠️ The operator cancel route has no UI — the history row / system-cleanup panel offer no "Cancel" button, so the fix is only reachable via API.
⚠️ Cancelled run renders the raw code "cancelled_by_operator" and "Failed"; a system run row also says "0 candidates" (file-cleanup wording).

#### #6540 — PASS
✅ Off: e2e-macos.local ▸ `#topology` → `topology-empty-state` "Network Topology is off — Breeze can build a live map… A partner admin can turn it on in Settings → Modules." + "Open Modules settings" link → `/settings/partner#modules`. Modules card ▸ Network Topology (Beta) ▸ switch "Enable network topology" (`partner-modules-topology`) → aria-checked true, toast "Modules updated", sub-options Physical topology / Interface health / Diagnostics / AI insights appear; DB `topologyFeatureFlags = {"ui":true,"materialization":true}`. Preparing: device Topology tab now shows "Building the topology map — Breeze is collecting network data for this site…" (spinner; no site graph on this stack). Unavailable: not reachable from real data (API `ui.reason` is only materialization_disabled / topology_preparing / ui_disabled), so forced by rewriting the `/topology/sites/:id/settings` response to `reason:'collection_unavailable'` → "Topology unavailable … contact support" with collapsed "Technical details" `<code>collection_unavailable</code>`. Topology left **ON** for the rest of the sweep.
⚠️ The Preparing state never re-checks — TopologyEntry fetches settings once on mount, so the "usually completes within a few minutes" spinner stays until the user reloads.

#### #6661 — PASS
✅ The UI surface is the configuration-policy Backup tab, not a Backup ▸ configs page: created policy "QA Backup Retention" (Default Organization) ▸ Backup tab ▸ "Advanced retention & timing" → amber note "Daily retention is set to 7 days, shorter than the 30-day retention floor. This snapshot's expiry is set to 30 days, not 7 -- expiry never shortens below the floor. A separate max-versions limit can still remove older snapshots sooner." GFS Daily 45 → note gone; 29 → note back with "29 days … 30-day floor". The server half (`computeExpiresAt` = max(tier, retentionDays)) needs a completed backup to observe; covered by `backupRetention.test.ts`.
⚠️ The note fires on the **defaults** (Standard 30-day preset + Daily 7) — every new backup policy opens Advanced with a warning the user didn't cause. Copy says "This snapshot's expiry" in a policy editor (no single snapshot) and uses "--" instead of a dash.

#### #6569 — PASS
✅ UI: policy "QA Backup Retention" ▸ Backup ▸ Destination ▸ "New destination" ▸ Amazon S3 / S3-Compatible, name "QA S3 MinIO", bucket/region/Access Key ID/Secret/Custom Endpoint ▸ Save → toast "Saved"; `backup_configs.provider_config` = `{bucket, region, endpoint, "accessKey":"QAACCESSKEY123","secretKey":"qa-secret-value-456"}`. API with the AWS spelling: `POST /backup/configs {details:{accessKeyId, secretAccessKey}}` → **201**, stored as `accessKey`/`secretKey` only (no `accessKeyId`/`secretAccessKey` keys); `PATCH` on the MinIO config with `accessKeyId:'ROTATEDKEY'` + masked secret → 200, stored `accessKey:"ROTATEDKEY"`, `secretKey` preserved, no stale second spelling. Agent-side dispatch not exercised (no agent).
⚠️ Chrome autofilled the saved **login** (admin@breeze.local / password) into "Access Key ID" / "Secret Access Key" when the New destination form opened — `autoComplete="off"` (BackupDestinationSection.tsx:651/670) is ignored for password fields; `autoComplete="new-password"` on the secret (and `off`/`one-time-code` on the key id) would stop it. A user who doesn't notice saves their Breeze credentials as S3 keys.
⚠️ Policy Backup tab ▸ Save with no backup path: error "Add at least one backup path before saving" renders ~1,400 px above the viewport; no scroll (this tab isn't among #6578's forms).

#### #6618 — PARTIAL
✅ Web half: seeded a backup job + snapshot + two `backup_verifications` rows on e2e-windows.local (SQL): `failed` with `details.reason` "QA agent error: checksum mismatch on 3 objects (sha256)" and `partial` with "QA agent error: 2 files locked during test restore". Device ▸ Backup ▸ Verification History (inline section, not a separate "Verification tab") shows the reason under **both** the Failed and the new Partial badge. API half (persisting the agent's error into `details.reason` from the scheduled-verification result) needs a live agent to post a result — BLOCKED there; covered by `verificationScheduled.test.ts`.

#### #6548 — PASS
✅ Seeded two `system_image` snapshots on e2e-windows.local: "QA restorable image" (`bare_metal_restorable=true`) and "QA NOT restorable image" (`false`, reasons "missing EFI system partition", "BitLocker volume not captured"). Backup ▸ Recovery Bootstrap (`/backup#recovery-bootstrap`; not under Devices ▸ Backup as the inventory says) ▸ Create recovery token ▸ NOT restorable + Bare metal ▸ "Create token" → POST `/backup/bmr/tokens` **409** `{"error":"snapshot_not_bare_metal_restorable","reasons":["missing EFI system partition","BitLocker volume not captured"]}`; `recovery_tokens` stayed empty. Same with the restorable snapshot → token minted (see #6580).
⚠️ The refusal is shown to the user as the raw code `snapshot_not_bare_metal_restorable` (error toast + inline banner); the `reasons` array the API returns — the actual "why" — isn't displayed.

#### #6580 — PASS
✅ BMR token: Backup ▸ Recovery Bootstrap ▸ "Create token" on the restorable snapshot → toast "Recovery token created." + inline "Recovery token created. Copy the CLI command before you leave this page."; `recovery_tokens` row `bare_metal/active`. Error path: the non-restorable 409 (#6548) surfaced as an error toast + banner (not silent). DR: `/dr` ▸ "Create Plan" ▸ "QA DR plan", group "Core", step "Bare-metal recover (boot media)", e2e-windows.local ▸ "Save plan" → toast "Recovery plan created.", dialog closes, row "QA DR plan · draft · 60m / 240m · 1". Edit ▸ RPO 30 ▸ Save → toast "Recovery plan saved.", row 30m / 240m.

#### #6578 — PARTIAL
✅ Backup ▸ Profiles ▸ New profile ▸ Blank ▸ "Save" (clicked at the bottom of the panel) → page scrolled so "Profile name is required" sits mid-viewport (top 431/877) and focus moved to the Name field wrapper; Save button now below the fold. DR ▸ Create Recovery Plan, dialog body scrolled to the bottom (scrollTop 107) ▸ "Save plan" → body scrolled to 0, "Plan name is required." banner visible and focused.
❌ BUG (UI-only, no API call): **second** invalid submit with the same error doesn't scroll. Scroll the DR dialog back down (scrollTop 169) ▸ "Save plan" again → scrollTop stays 169, the banner is at top −31 px (above the dialog body's top edge at 32 px) — reads as a no-op, the exact symptom #6494 fixed. Cause: `DRPlanEditor.handleSave` calls `setError(undefined)` then `setError('Plan name is required.')` in the same handler; React batches them, so `useScrollToError` (`apps/web/src/lib/scrollToError.ts:49`) sees the same string and skips. BackupProfilesTab calls `scrollErrorIntoView` directly from validate() and is not affected. DeviceBackupTab's banner path needs an agent-backed action to error; not exercised.

#### #6566 — BLOCKED
BLOCKED: needs a live agent whose WebSocket closes/errors so the handler's offline write can be observed; both seeded devices are already `offline` and there is no agent on this stack. `/devices` list renders both as Offline from the DB status. Covered by the API unit tests in the PR.

<!-- end devices-backup-topology -->

### Monitoring, security & AI — 2026-09-23 00:35Z

#### #6558 — PASS
✅ Policy ▸ Monitors tab ▸ "Create monitor" ▸ Kind "Network check", Check type "HTTP check", Target `http://example.com/old`, "Expected status code" 301 ▸ "Save monitor" → back on the policy tab with "QA redirect check" attached. Compiled `network_monitors.config` = `{"expectedStatus": 301, "followRedirects": false}`. Control (same kind, expectStatus 200, via POST /monitor-definitions 201) compiles to `{"expectedStatus": 200}` — no key, agent default. The editor has no "Follow redirects" control at all (inventory wording); the default is compiler-side only.
⚠️ Switching Check type from ICMP ping to HTTP check keeps the ping default Target `8.8.8.8` (no scheme) — easy to save an HTTP check against a bare IP.

#### #6556 — PASS
✅ Created cpu monitor "QA CPU delete-me" (compiled alert_rule f5fa0e3a…), seeded an `alerts` row pointing at both that rule_id and monitor_id. `/alerts/monitors/<id>` ▸ "Delete" ▸ confirm → toast "Monitor deleted", redirected to `/alerts/monitors`. Alert row survives with `rule_id` and `monitor_id` both NULL; FK is now `ON DELETE SET NULL`.

#### #6564 — PASS
✅ Deleted the attached "QA redirect check" from its editor (toast "Monitor deleted"). `config_policy_monitors` now empty while the link's `inline_settings` mirror still names the deleted id (the exact bug precondition). Reopened policy ▸ Monitors tab: "No monitors attached to this policy yet.", no `monitors-tab-item-*` rows, no bare UUID.

#### #6480 — PASS
✅ No web UI calls the conversion routes (grep of apps/web/src finds no `/conversion` caller; policy ▸ Alerts tab says "New rules are created as monitors / Existing rules … convert to monitors in the next release"), so exercised the API from the logged-in browser session. Seeded a legacy inline rule "QA legacy high CPU" (cpu gt 85) on the policy's `alert_rule` link. `GET /monitor-definitions/conversion/policies/<id>/preview` → 200, 1 item outcome `convertible`, previewHash returned (before the fix this answered 404 `policy_not_found`). `POST …/convert {previewHash}` → 200 `{"retired":1,"monitorsCreated":1}`; source row `retired_reason=operator`, `converted_to_monitor_id` set. After a full page reload, policy ▸ Monitors tab lists "QA legacy high CPU · cpu · high".

#### #6615 — PARTIAL
✅ Inline preview path (above) returns 200 with the caller's auth. The fix itself is in `restorePreviewAuth`, which only runs in `monitorConversionPreviewWorker` — the queued path taken when the policy resolves to more than 500 devices (`EQUIVALENCE_JOB_THRESHOLD`). Not exercised: that means seeding 500+ devices into the shared sweep stack, which would disrupt the other groups. There's also no web "Preview" button (inventory click path doesn't exist).

#### #6573 — PASS
✅ Sidebar "IOC Scans" → `/security/scans`: header "IOC Scans" + disclaimer, Scans/Threats tabs (hash-routed, `#threats`). Scans tab lists both e2e devices. Selected e2e-windows.local ▸ "Start scan" → toast "1 scan queued"; Active scans shows "Quick Scan · Status: Queued"; `device_commands` got a pending `security_scan`. Threats tab: seeded a `security_threats` row, "Refresh" → row listed; selected it ▸ bulk Quarantine → toast "1 threat queued", row status "quarantined", pending `security_threat_quarantine` command. Policy ▸ Security tab: only Scan Settings (type / max file size / timeout), Auto-quarantine, server-dispatched Scheduled Scans and Exclusions; the five dead toggles are gone. Added exclusion `C:\QA\Trusted`, max size 75 ▸ Save → toast "Saved"; `inline_settings` = `{scanType: quick, maxFileSizeMb: 75, exclusions: ["C:\\QA\\Trusted"], scheduledScans: true, scanHour: "2", …}`.
⚠️ `ThreatDetail` is still not mounted anywhere (only `ThreatList` renders; the PR body says all three were mounted). Clicking a threat row does nothing, so a quarantined threat can't be **restored** from the UI.
⚠️ The threat row flips to "quarantined" the moment the command is queued, even on an offline device (the toast says "queued"). Status/severity cells are raw lowercase (`active`, `high`) while the filters say "Active"/"High". The count reads "1 threats match your filters" (no plural form). The auto-quarantine switch has no accessible name.

#### #6562 — PASS
✅ The stack has a working Anthropic key, so chat is live. Settings ▸ Organizations ▸ Default Organization ▸ **AI** (`/settings/organizations/<id>#ai`) ▸ "Max Turns Per Session" = 7 ▸ "Save Budget" → toast "Saved successfully"; `ai_budgets.max_turns_per_session = 7`. `/workspace` ▸ "New Conversation" ▸ sent 7 one-word prompts, all answered; new `ai_sessions` row has `max_turns = 7` (the schema default is 50). The 8th send → POST `/ai/sessions/<id>/messages` **400** `{"error":"Session turn limit reached (7)"}`, and the chat shows a dismissible "Session turn limit reached (7)" banner. A second new session also got `max_turns = 7`.
⚠️ When the limit is hit, the refused prompt still shows in the transcript as if it was sent. The banner sits at the top of the thread, far from the composer, and doesn't point to "New" (starting a new chat).

#### #6557 — PASS
✅ New chat ▸ asked the assistant to run `system_cleanup` on the offline e2e-windows.local. The tool card shows "System cleanup · **Failed**" (`ai-tool-failed`), and below the collapsed header, without expanding, `ai-tool-failed-reason` reads "Device e2e-windows.local is not online (status: offline). This tool needs a live connection; …".
⚠️ The card header repeats the label ("System cleanup / System cleanup").

#### #6559 — PASS
✅ Seeded partner user `qa-viewer@breeze.local` (same password as admin) with the seeded **Partner Viewer** role, which has no `ai_sessions:use`. Logged in from a separate browser context. Sidebar AI group shows only "Fleet Orchestration" and "AI Usage": no `/workspace` link, and the header "AI Assistant" button is gone. Going straight to `/workspace` renders `workspace-access-denied` ("Access denied — You don't have permission to view this."), with no composer and no "New Conversation". Control: admin (Partner Admin) sees the sidebar "AI Assistant" link and the full workspace.
⚠️ The viewer also gets the "MFA is not required for your team… Require MFA" banner, linking to partner settings they can't change.

#### #6600 — PARTIAL
✅ The UI half renders. Seeded a `ticket_checklist_items` row with `source='operator_task'` on ticket QTDI9LZA5V: the ticket's Checklist card shows "QA operator human-work step" with an "Operator step" badge (`ticket-checklist-operator-badge-*`, tooltip "Created by an AI Operator task. Tick it when the work is done and the task continues."), and the checkbox stays enabled. No "view task" link, because no real step or task row exists.
BLOCKED for the end-to-end: the stack runs with `AI_OPERATOR_TASKS_ENABLED=false`, and there's no recipe that dispatches a `human_work` step, so the operator never creates an item and the tick never wakes a task.

<!-- msa-end -->

### Settings, ticketing & portal — 2026-09-23 00:50Z

#### #6534 — PARTIAL
✅ There's no "full-message replies" toggle in the UI. `InboundEmailCard.tsx:27-30` says so ("Not yet edited by this card"), so the inventory click-path is wrong: the setting can only be changed through the API. What I checked instead: set `ticketing.inbound.fullMessageReply=true` with `PATCH /orgs/partners/me` (200), then saved from the card by toggling "Send an autoresponse…". The toast "Inbound email settings saved" appeared, and `GET /ticket-config` still returned `fullMessageReply: true`, so a card save doesn't drop the flag. Restored afterwards (flag false, autoresponder on).
BLOCKED for the rest: the flood backpressure and loop/bounce suppression need a live inbound path. The stack has no `TICKETS_INBOUND_DOMAIN` or Mailgun (the card says "No platform inbound domain is configured").
⚠️ STP-1: the opt-in full-message reply mode has no UI control. A partner can only turn it on through the API.

#### #6553 — PASS
✅ Connect: "Microsoft 365 support mailbox" ▸ Mailbox address ▸ "Connect" → `POST /tickets/mailbox/connect` 400 `{"error":"M365 ticket mailbox app is not configured"}`, not a 500. The stack has no mailbox app secret configured, so the FK path from the fix (consent-session insert on the request tx) wasn't reached live.
✅ Admin-consent error redirect: seeded a `pending_consent` connection and an `admin_consent` consent session via SQL, set the HMAC-signed `ticket_mailbox_oauth_state` binding cookie, then opened `/api/v1/tickets/mailbox/callback?error=invalid_grant&error_description=AADSTS50097…&admin_consent=True&state=…`. The callback returned 302 → `/settings/ticketing?ticketMailbox=error#email`, not the old 400 "Invalid OAuth callback parameters". The page showed the error toast "M365 connection failed" and stripped the query param. The connection moved to `reauth_required` with last_error "Mailbox verification failed", and the session was consumed. The audit row `ticket_mailbox.verification_failed` has outcome `invalid_identity` and no AADSTS text. Seed row deleted.
⚠️ STP-2: after a denied *first-time* consent, the card shows "Re-auth required — Administrator re-consent is required before Microsoft 365 polling and replies resume" for a mailbox that was never connected. The toast is also generic and doesn't say that consent was refused.

#### #6542 — PASS
✅ Org ▸ Ticketing (`/settings/organizations/<id>#ticketing`, `OrgTicketSettingsEditor`): each SLA cell renders `InheritedField`, with the direction note "A ticket's category SLA takes priority over these org-level overrides when both are set." To test the indicator, I set partner Normal response = 480 at `/settings/ticketing#priorities` (toast "Priorities and SLAs saved"). The org cell then read "Inherits from Partner default" with placeholder `480`. Typing 120 changed it to "Overrides Partner default (480)", and fields with no partner value read "No Partner default configured". Reverted the partner value (toast shown again).
✅ Org ▸ Billing (`#billing`, `OrgBillingSettings`): the Tax rate field shows the same indicator ("No Partner default configured").
✅ Inbound Email is split out: `/settings/ticketing#email` shows the "Inbound email" section (own "Enable email-to-ticket" toggle, triage org, unknown-sender radios, autoresponder), then "Inbound address" and "Customer email domains". The "Microsoft 365 support mailbox" card below has its own Connect.
⚠️ STP-3: Urgent/High SLA cells say "No Partner default configured" even though built-in defaults apply (60/240 and 240/1440 min, which the partner page shows as placeholders). Settings rule 4 says the field shows the inherited value and its source.
⚠️ STP-4: the org Ticketing form's unsaved-changes guard stays armed after you type a value and clear it back to blank. Navigating away raises a `beforeunload` prompt with nothing to save.

#### #6565 — PASS
✅ Tested through the API, with the same calls the "Add passkey" UI makes. `POST /auth/passkeys/register/options` with `{currentPassword}` returned 200 with a challenge (rp.id `localhost`). I then sent `POST /auth/passkeys/register/verify` with a forged `clientDataJSON` (correct challenge, `origin: "https://evil.example.com"`). Result: **400** `{"error":"Passkey registration failed","message":"Passkey registration failed","code":"mfa_proof_invalid"}`. The body contains no origin or RP ID. The library detail went only to the server log: `[passkeys] registration verification rejected: Error: Unexpected registration response origin "https://evil.example.com", expected "http://localhost:33032"`.
Side observation (not #6565): the stack's expected WebAuthn origin is `http://localhost:33032`, not the caddy URL `:32793` that the sweep browses. A real passkey ceremony at `:32793` would fail the origin check on this stack.

#### #6532 — PASS (with a pre-existing ❌ bug in the clear path)
✅ Platform admins are exempt from the allowlist (`ipAllowlist.ts:36`), so I created a temporary non-admin Partner Viewer `qa6532@breeze.local` via SQL. Baseline: its `GET /devices` returned 200. Then Settings ▸ Partner ▸ Security ▸ IP Allowlist = `203.0.113.0/24` ▸ "Save Settings". The lockout confirm appeared ("Your current IP is not in this allowlist. Saving may lock you out of the dashboard. Continue?") and I accepted it. Status then read `enforced:true, active:true`. The viewer's `GET /devices` and `POST /auth/login` both returned **403** `{"code":"ip_not_allowed","error":"Access denied from this IP address"}`, and the exempt admin still got 200. So the denial UX is unchanged with the pre-transaction check.
❌ BUG (pre-existing, not a #6532 regression; blame dates to the 09-11 shallow root): **you can't clear the partner IP allowlist from the UI.** Deleting all lines and clicking "Save Settings" shows the toast "Partner settings saved", but the allowlist stays enforced. The PATCH body contains `"security":{"requireMfa":false}` with no `ipAllowlist` key, because `PartnerSecurityTab.tsx:149` sets `ipAllowlist: lines.length > 0 ? lines : undefined` and JSON drops `undefined`. `routes/orgs.ts:269` only clears on an explicit value ("explicit value (incl. []) wins"), so it carries the old list forward. DB after the "clear": `settings->'security'->'ipAllowlist'` = `["203.0.113.0/24"]`, and the viewer still got 403. A partner who locks out its own non-admin users can't undo that from the dashboard. Fix: send `[]` instead of `undefined` on an empty textarea. I cleared the list via SQL; access came back once the 30 s in-process cache expired. The test user is disabled.

#### #6579 — PASS
✅ Inserted a pending `signup` token into `email_verification_tokens` (sha256 hash, for the temporary `qa6532` user). Opened `/auth/verify-email?token=…` in a fresh logged-out browser context. After 4 s the page showed "Confirm your email — Click the button below to confirm your email address and finish setting up your account." with a "Confirm my email" button, and the page had made **zero** `/api/` requests. Clicking the button sent one `POST /api/v1/auth/verify-email`, and the page changed to "Email verified — Thanks for confirming your email… Sign in". `consumed_at` was set only after the click.

#### #6623 — PASS (API; the UI page needs a live IdP)
✅ The inventory's expectation ("instead of a generic 500") is inaccurate. The fix adds `code: INVALID_CREDENTIALS` to `genericAuthError()`, which `POST /sso/link/confirm` uses (the SSO account-link password step). To reach that step without an IdP, I seeded an `sso:pendinglink:<sha256>` record in Redis for the temporary `qa6532` user, then POSTed a wrong password with the `breeze_sso_pending_link` cookie. Result: **401** `{"error":"Invalid email or password","code":"INVALID_CREDENTIALS"}`, so the SSO path now carries the code, and web `apiSsoLinkConfirm` shows it via `extractApiError`. Record deleted afterwards. The browser page for the link step (after an IdP round-trip) wasn't reached, because the stack has no SSO provider.

#### #6533 — PARTIAL (with a pre-existing ❌ bug on the return path)
✅ The entry point renders at `/integrations#m365`, showing the "Customer Graph Read" and "Customer Graph Actions" cards with their permission lists and Connect. On this stack both say "…onboarding is not enabled for this organization": the org isn't in `M365_CUSTOMER_GRAPH_READ_ONBOARDING_ORG_IDS`, so Connect can't start and I couldn't observe the new `navigateToMicrosoftLogin` hard-navigation live.
✅ Callback now tolerates Microsoft's shape: `GET /api/v1/m365/consent/callback?admin_consent=True&tenant=<guid>&state=…` with no browser binding returned 302 → `/integrations#m365/customer-graph-read/consent_state_mismatch`, not a 400. The API also logged the new diagnostic `[m365ConsentCallback] browser binding missing or invalid {profile:'customer-graph-read', correlationId, cookieHeaderPresent:true}`, with no state or payload in it.
❌ BUG (pre-existing: `IntegrationsPage.tsx:234-283` blames to the 09-11 root): **the callback outcome banner never appears after a real (full-page) return.** After the 302, or any cold load of `/integrations#m365/customer-graph-read/consent_state_mismatch` (tested after `about:blank`), the hash is rewritten to `#m365` within 500 ms, and "The consent session could not be verified. Start consent again." never renders. It *does* render and persist when you only change the hash (same-document navigation from `/integrations#m365`). Root cause (inferred, not traced): the layout effect stores the result with `orgId: callbackOrgId` while that is still null on first paint, then `replaceState`s to `#m365`. When `callbackOrgId` resolves, the effect re-runs (dep `[callbackOrgId, initialTab]`), re-parses `#m365`, and clears the result. The same path would also drop the "active" success banner after a real consent.
BLOCKED for the rest: seeing the status flip to Active needs a live M365 tenant and the org in the onboarding allowlist.

#### #6235 — PASS
✅ Created a "Partner library" policy "QA 6235 partner filters" through the New Configuration Policy wizard ("Configure New" ▸ Scope "Partner library"), then opened ▸ More ▸ Assignments. The Organizations panel now shows "Role Filter (optional)" chips (Workstation … Unknown), "OS Filter (optional)" chips (Windows/macOS/Linux), and "These filters apply when you assign below…". I chose Workstation + Windows and ticked "Default Organization": `POST …/assignments` 201 with body `{"level":"organization","targetId":…,"roleFilter":["workstation"],"osFilter":["windows"]}`. The org moved under "Assigned" with "Workstation" and "Windows" badges. After `location.reload()` the badges were still there and the chips were re-seeded from the saved row. `GET …/assignments` returned `[('organization',['workstation'],['windows'])]`. Test policy deleted (200).
⚠️ STP-7: the assign/unassign toggle gives no toast. The row moving to "Assigned" is the only confirmation.

#### #6175 — PASS
✅ Settings ▸ Organizations ▸ Default Organization ▸ Customer Portal (`#portal`, `OrgPortalSettingsEditor`) has a "Network visibility" toggle (`org-portal-toggle-enableNetworkVisibility`, "Show read-only network health and discovered assets in the customer portal."), off by default. Checking it and clicking "Save portal settings" showed the toast "Portal settings saved", and it stayed checked after `location.reload()`. Unchecking and saving also persisted (same toast).

#### #6646 — PASS
✅ Portal as `portal@breeze.local`, flag on: the sidebar shows "Network" (`portal-nav-network`) between Reports and Profile, and `/portal/network` renders "Network — Reachability and monitoring status for your network assets." With no assets it shows "No network assets have been discovered yet." After seeding 2 `discovered_assets` rows via SQL it shows the ledger: Total assets 2, Online (verified) 0, Offline (verified) 0, SNMP devices polling 0, Monitors down 0, plus the footnote "Online and offline counts exclude unverified assets…". The online/offline counts are 0 because no reachability probe has verified the seeded rows, which is correct. With the flag off, the nav entry disappears and `/portal/network` redirects to `/portal/quotes`. Seed rows deleted and the flag left off (its default).

<!-- stp-end -->

## UI/UX paper cuts

| # | Where | Observation | Severity | Disposition (fixed <sha> / issue # / noted) |
|---|---|---|---|---|
| BQ-1 | Quote detail ▸ Details | Issued date shows 9/23 while Created/Accepted show 9/22 for the same instant (likely UTC vs local rendering — not traced) | low | noted |
| BQ-2 | Accept on behalf | Auto-email / on-behalf notice send failure is only logged server-side; tech sees plain success toast | low | noted |
| BQ-3 | Quote ▸ declined banner | Decline-on-behalf banner omits who recorded it / method / reference and calls the tech-entered reason "Customer's note" | low | noted |
| BQ-4 | Ticket ▸ Time & Billing | Missing-rate note reads "not counted in the amount above" but sits above the Time amount row | low | fixed 2ff995ad48 |
| BQ-5 | Timesheet row | Hourly rate rendered twice ("$110.00/h" badge + bare "$110.00" column) — the bare one reads as the entry amount | low | noted |
| BQ-6 | Timesheet edit | After a 404 on save (entry deleted elsewhere) the row stays in edit mode and the deleted entry is still listed | low | fixed 2ff995ad48 |
| BQ-7 | Ticket ▸ Parts card | After a 404 on edit/delete (part already gone) the card doesn't refetch and keeps listing the part | low | fixed 2ff995ad48 |
| DBT-1 | Device ▸ Filesystem ▸ System cleanup | 409 agent_update_required shown twice (error toast + identical inline banner) | low | noted |
| DBT-2 | Device ▸ Filesystem ▸ Cleanup history | New system-run cancel route (#6602) has no UI button; cancelled row shows raw "cancelled_by_operator"; system runs say "0 candidates" | low | noted |
| DBT-3 | Device ▸ Topology (Preparing) | "Building the topology map… within a few minutes" never re-checks; stays until manual reload | low | noted |
| DLT-1 | Ticket ▸ inbound-email reopen (#6711/#6715) | `ticket.reopened` ML feedback row from an inbound reply carries `metadata.source: "manual_update"` and null actor — indistinguishable from a tech reopen for the triage model | low | noted |
| DBT-4 | Config policy ▸ Backup ▸ Advanced | Retention-floor warning fires on the untouched defaults (Standard 30 d + Daily 7); copy says "This snapshot's expiry" and uses "--" | low | noted |
| DBT-5 | Config policy ▸ Backup ▸ New destination | Chrome autofills the saved Breeze login into Access Key ID / Secret Access Key (`autoComplete="off"` ignored on password inputs) | medium | fixed b1d2e04535 |
| DBT-6 | Config policy ▸ Backup ▸ Save | "Add at least one backup path before saving" renders ~1,400 px above the viewport; no scroll | low | noted |
| DBT-7 | Backup ▸ Recovery Bootstrap | Non-restorable refusal shows raw `snapshot_not_bare_metal_restorable`; API's `reasons` array not displayed | medium | fixed b1d2e04535 |
| DBT-8 | DR ▸ plan editor | Repeat invalid Save with the same error message doesn't scroll (batched setError undefined→same string defeats useScrollToError) | medium | fixed b1d2e04535 |
| MSA-1 | Monitor editor ▸ Network check | Changing Check type to HTTP check keeps the ICMP default Target `8.8.8.8` (no scheme) | low | fixed 50deca1d9e |
| MSA-2 | Config policy ▸ Monitors / Alerts | Legacy→monitor conversion (preview/convert) has no web entry point; the Alerts tab only says rules "convert to monitors in the next release" | low | noted |
| MSA-3 | Security ▸ IOC Scans ▸ Threats | `ThreatDetail` never mounted — rows aren't clickable, so there's no restore path for a quarantined threat | medium | fixed 50deca1d9e |
| MSA-4 | Security ▸ IOC Scans ▸ Threats | Row shows "quarantined" as soon as the command is queued (offline device); raw lowercase status/severity; "1 threats" plural | low | issue #6685 |
| MSA-5 | Config policy ▸ Security | Auto-quarantine toggle button has no accessible name/label | low | noted |
| MSA-6 | AI Workspace ▸ turn limit | Refused 8th prompt stays in the transcript as if sent; the limit banner is at the top of the thread, not by the composer, with no "start a new chat" hint | low | noted |
| MSA-7 | AI Workspace ▸ tool card | Tool card header shows the label twice ("System cleanup / System cleanup") | low | noted |
| STP-1 | Settings ▸ Ticketing ▸ Email ▸ Inbound email | Opt-in full-message reply mode (#6534) has no UI control; API-only | low | noted |
| STP-2 | Settings ▸ Ticketing ▸ Email ▸ M365 mailbox | Denied first-time admin consent shows "Re-auth required / re-consent is required before polling resumes" for a never-connected mailbox; toast just "M365 connection failed" | low | noted |
| STP-3 | Org settings ▸ Ticketing ▸ SLA overrides | Urgent/High show "No Partner default configured" though built-in 60/240 min defaults apply (rule 4: show inherited value) | low | noted |
| STP-4 | Org settings ▸ Ticketing | beforeunload guard stays armed after typing then clearing a field back to blank | low | noted |
| STP-5 | Settings ▸ Partner ▸ Security ▸ IP Allowlist | Emptying the allowlist + Save shows "Partner settings saved" but the list stays enforced (web sends no `ipAllowlist` key; API keeps the old list) — pre-existing bug, see #6532 entry | high | fixed 06acdabe60 |
| STP-6 | Integrations ▸ M365 ▸ Customer Graph Read/Actions | Consent-callback outcome banner (error or "active") never shows on a full-page return — hash rewritten to #m365 and result cleared when callbackOrgId resolves (pre-existing, see #6533 entry) | medium | issue #6684 |
| STP-7 | Config policy ▸ Assignments ▸ Organizations | Assign/unassign org toggle has no toast; only the row moving under "Assigned" confirms | low | noted |
| MSA-8 | Global MFA banner | Read-only Partner Viewer sees "Require MFA" nudge linking to partner settings they cannot edit | low | noted |
| FS-1 | Settings ▸ Partner ▸ Security ▸ IP Allowlist | Enter at end of a line is swallowed (per-keystroke re-parse drops the empty line); multiple entries can only be pasted, and typed ones concatenate (`10.1.1.110.2.2.2`) — pre-existing | medium | noted |
| FS-2 | Invoice preview / PDF / portal | Ticket-less lines (manual line) render straight after the last ticket group with no header/divider, so they read as billed on that ticket | medium | noted |
| FS-3 | Portal ▸ invoice detail | Repeats "Ticket #X" under every line in addition to the group header | low | noted |
| FS-4 | Reports ▸ Saved Reports (All orgs) | Partner-owned business report shows raw key `reports.reportsList.reportTypes.ticket_sla_attainment`; "Generate now" toasts raw `unsupported_report_scope` | low | noted |
| FS-5 | Devices ▸ Groups ▸ Delete | Modal copy "remove 0 device s from the group"; no success toast (allowlisted) | low | noted |
| FS-6 | IOC Scans ▸ threat detail | Quarantine button enabled on an already-quarantined threat | low | noted |

## Fixes applied

| Commit | PR/area | What | Test |
|---|---|---|---|
| 2ff995ad48 | #6581 / #6567 / #6588 / #6613 (BQ-4, BQ-6, BQ-7) | Missing-rate note moved below the amount it refers to; timesheet + parts card exit edit/confirm mode and refetch on a 404 | TicketTimeBilling / TimesheetPage / TicketPartsCard tests — red first, 70/70 |
| b1d2e04535 | #6578 / #6569 / #6548 (DBT-8, DBT-5, DBT-7) | DR editor re-scrolls on every failed save; S3 secret `autoComplete=new-password`; BMR non-restorable refusal in plain copy + reasons list | DRPlanEditor / BackupDestinationSection / RecoveryBootstrapTab tests — red first (DRPlanEditor "#6494 focus" test fails locally on the pre-fix code too — pre-existing, not this change) |
| 50deca1d9e | #6573 (MSA-3), monitors (MSA-1) | Mount `ThreatDetail` on the IOC Threats tab (row click/keyboard → detail with restore); HTTP check no longer inherits the ping default target | SecurityScansPage / MonitorConditionFields tests — red first |
| 06acdabe60 | pre-existing, found via #6532 (STP-5) | Clearing the partner IP allowlist sends `[]` — before, the save toasted success and the allowlist kept blocking | PartnerSecurityTab test — red first, 55/55 in settings |

## Issues filed

| Issue | Title | From row |
|---|---|---|
| #6684 | M365 consent result banner never renders after a real return from Microsoft | #6533 / STP-6 (pre-existing) |
| #6685 | IOC threats show 'quarantined' as soon as the command is queued | #6573 / MSA-4 |

## Summary

| Group | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|
| Billing & quotes | 12 | 3 | 0 | 1 | 0 |
| Devices, backup & topology | 8 | 2 | 0 | 1 | 0 |
| Monitoring, security & AI | 8 | 2 | 0 | 0 | 0 |
| Settings & admin | 6 | 1 | 0 | 0 | 0 |
| Ticketing & mailbox | 1 | 1 | 0 | 0 | 0 |
| Portal | 2 | 0 | 0 | 0 | 0 |
| No UI surface (deps/docs/test/CI, api-/agent-only) | 0 | 0 | 0 | 0 | 50 |
| **Total (98)** | **37** | **9** | **0** | **2** | **50** |

**Top findings:**
- No row FAILed. Every web/portal change since v0.115.0 does what its PR says in the browser.
- **Shipped-but-unreachable UI** recurs: `ThreatDetail` (#6573) was never mounted (fixed), and three backend features have no web entry point — the system-cleanup cancel route (DBT-2), legacy monitor conversion (MSA-2), and inbound full-message replies (STP-1).
- **Pre-existing, serious:** the partner IP allowlist could not be cleared from the UI (STP-5, fixed) — a partner who locks out non-admin users could not undo it from the dashboard.
- **Stale-state 404s** now surface correctly (#6567 #6588 #6613) but left ghost rows in edit mode (fixed).

**Oldest PR reached:** #6468 (every PR in `v0.115.0..70868c3890`).

**Before the release cut:**
- Merge the sweep PR (4 fix commits + this doc).
- BLOCKED / PARTIAL rows needing live infra: #6566 (agent WS disconnect → Offline), #6618 API side (agent verification error), #6616 (TS/SQL billed-minutes disagreement — unreachable from UI), #6600 (AI Operator human_work step, `AI_OPERATOR_TASKS_ENABLED`), #6615 (queued preview path, >500 devices), #6533/#6553/#6623 real Microsoft / IdP round-trips, #6534 flood/loop suppression (inbound mail domain).
- Agent-only changes (#6547 #6545 #6546 #6606 #6491) ship with the 0.116.0 agent; #6643 already gates system cleanup on it.

---

## Final sweep @ release cut `d815846588` (2026-09-23)

Stack: `pnpm wt-stack up` on `qa/final-sweep-0.116.0` @ `d815846588`, baseUrl `http://localhost:32835`. Covers everything merged after the first sweep base (`70868c3890`) plus the live-agent rows the first pass left BLOCKED/PARTIAL.

| PR | Title | Surface | Status |
|---|---|---|---|
| #5856 | Group invoice lines by ticket and category (PDF, portal, web) | web/portal | FAIL — portal category null (RLS, partner-axis `ticket_categories`); shipped as known gap → #6674 |
| #5855 | Clean up config policy assignments before deleting a device group | web | PASS |
| #5489 | Ticket mailbox: live Graph role check when wids absent | api (M365) | BLOCKED |
| #6673 | Partner-scope reports foundation (W01) | api/web | PASS |
| #6686 | Sweep fixes (BQ-4/6/7, DBT-5/7/8, MSA-1/3, STP-5) — re-verify at cut | web | PASS |
| #6704 #6706 | DNS threat alerts only for threat categories; adult_content before adware | api | PARTIAL |
| #6712 | What's New 0.116.0 splash | web | PASS |
| #6671 #6679 | Topology flag resolution before org tx / site-state row lock (heartbeat) | live agent | PASS |
| #6566 | Offline transition persisted on agent WS close | live agent | PASS — found double `device.offline` publish, fixed pre-tag in #6722 |
| #6618 | Agent verification error persisted as failure reason | live agent | PASS |
| #6705 | Windows System-log errors classified hardware/system | agent (Windows) | PARTIAL |
| #6678 #6682 #6681 | Anomaly episodes table; partner-export trigger lock; docs | no UI | N/A |

### Final sweep log

Live-agent rig (#6566 #6618 #6671/#6679): Go agent built from `d815846588` (`GOOS=linux GOARCH=arm64`, `-X main.version=0.116.0`) run in `ubuntu:22.04` container `qa-agent-0116` on the stack network, enrolled at `http://172.31.2.10` with a SQL-minted enrollment key + `AGENT_ENROLLMENT_SECRET` → device `abbe1328` (hostname `qa-agent-0116`, Default Organization) online, WS connected. MinIO `qa-minio-0116` on the stack network for #6618.

#### #6566 — PASS ⚠️
✅ `docker stop` (SIGTERM): API logged `Agent 302f5cff… disconnected` at 02:56:17.353 → `devices.status='offline'` 0.14 s after the stop returned (DB-polled), `offline_transition_effects` got 2 rows (`alert-plan`, `offline-event`) keyed `offline-transition-<hash>` — i.e. the WS close handler ran `transitionDeviceOffline`, not the 5-min sweeper. `docker kill` (SIGKILL): offline 0.17 s after the kill. `docker start` + agent start → `online` in 1.3 s (`device.online` published).
⚠️ **Duplicate `device.offline` per disconnect (regression from #6566).** Every WS close published `device.offline` twice ~40–70 ms apart: once from `agentWs.ts:3515` (`publishEvent(..., 'agent-ws')`, random event id e.g. `51f24370…`, `8db7e223…`) and once from the `offline-event` effect that `transitionDeviceOffline` → `persistOfflineTransition` now enqueues (`offlineTransitionEffects.ts:27`, source `offline-detector`, deterministic id `da619d5b…`, `a9152c57…`). Before #6566 the WS path called `updateDeviceStatus` (no effect row) so it published once. Distinct event ids → webhooks / `device.offline` automations fire twice. Fix: drop the direct `publishEvent('device.offline')` in both agentWs close branches (the effect is the durable publisher).

#### #6671 #6679 — PASS
✅ Set `partners.settings.topologyFeatureFlags` = all six flags `true` on the default partner (SQL, 02:57:27Z — left ON). Agent heartbeated every 60 s for ~8 min: 6/6 `POST /agents/:id/heartbeat` 200, 398–1018 ms (first post-enable beat 1018 ms, same as the first beat before enabling, 985 ms). The topology path ran: `topology_site_state` row for Default Site updated at 02:59:19 (that heartbeat), `topology_collection_runs` = 8. `pg_stat_activity` sampled every 3 s for 4 min (03:00–03:04): zero `wait_event_type='Lock'`, no active query > 1 s, and only momentary `idle in transaction` (xact age ≤ 0.5 s, between statements — none on `topology_site_state`/`partners` held). API log since the enable: no errors, deadlocks, lock or pool timeouts.

#### #6618 — PASS
✅ Backup config "QA-0116 MinIO" (s3, endpoint `http://qa-minio-0116:9000`, bucket `qa-backup-0116`) + org config policy with a backup feature link (paths `/etc`) assigned to the device (SQL). `POST /backup/jobs/run/:deviceId` → job `0e093287` **completed**, snapshot `snapshot-20260923T025856Z-c986968f`, 401 files in MinIO. Deleted `…/manifest.json` with `mc rm`, then `POST /backup/verify {backupJobId, verificationType:"integrity"}` → verification `78659ce7` **failed**, `details.reason` = the agent's text `manifest not found: backup provider: object not found: operation error S3: GetObject, … StatusCode: 404 … NoSuchKey: The specified key does not exist.`, `details.source='agent.result'`. `GET /backup/verifications?deviceId=…` returns `details: {reason: "manifest not found: …"}` (length-capped, no other keys).
ℹ️ Pre-existing (not this sweep): `POST /backup/jobs/run` logs the #1105 guard `bullmq.add(backup) ran inside a held withDbAccessContext transaction` on every manual run. The first manual run fell back to the org's other config (`E2E Default Backup`, local) and failed "no paths are configured" — fallback picks `LIMIT 1` without honouring `isDefault`.

#### #6705 — PARTIAL
✅ Classifier file carries no build tag, so it runs on macOS: `go test ./internal/collectors/ -run 'TestClassifySystemLogEntry|TestSystemLogCategoryFor|TestSystemLogQueryEnabled' -count=1` → 3/3 PASS at the cut; `GOOS=windows go vet ./internal/collectors/` clean. BLOCKED for live: Windows host (lab VM) to confirm System-log rows land as `hardware`/`system`.

#### #6712 — PASS
✅ Web build on the stack is `dev` (sidebar "Web dev"), so `decideWhatsNew` returns no entry by design; opened via sidebar "What's new" (reopen path = `latestApplicableEntry`, highest entry): heading "What's new in 0.116.0", subtitle "Accept quotes on a customer's behalf, IOC threat scanning, and invoices grouped by ticket", 5 `<li>` highlights, Learn more / Show me later / Got it. "Got it" closed it and wrote `breeze.whatsNew.lastSeenVersion=0.116.0`. Auto-show on a versioned build (web version vs stored floor) not browser-forceable (WEB_VERSION is baked at build) → covered by `whatsNewState.test.ts` + `WhatsNewSplash.test.tsx` 22/22 at the cut.

#### #6704 #6706 — PARTIAL
✅ No ingest endpoint: DNS events arrive only via `dnsSyncJob` pulling from a provider integration (Umbrella/Cloudflare/DNSFilter creds) — none on the stack. Unit suites at the cut: `dnsSyncJob.test.ts` (normalizeThreatCategory table incl. "Adult Themes"→adult_content, "Trading"/"Download"→unknown, "Ads"→adware), `dnsSyncJob.threatGate.test.ts` (content/unknown blocks publish nothing), `dnsThreatAlerts.test.ts` (`not_threat_category` gate), `dnsSecurity.test.ts` — 4 files / 62 tests passed. BLOCKED for live: a connected DNS security integration.

#### #5855 — PASS
✅ Devices ▸ Groups ▸ Create Group "QA-5855 Group" (c3b42f97) → new config policy "QA-5855 Policy" (org-owned) ▸ More ▸ Assignments ▸ Level Device Group ▸ target QA-5855 Group ▸ Assign → row `config_policy_assignments` fa1f43ed (level=device_group, target=c3b42f97). Groups ▸ Delete ▸ "Delete group": modal closed, card gone from list; DB: 0 assignment rows for the target, 0 `device_groups` rows. No FK error.
⚠️ No success toast on delete — by design (`DeviceGroupsPage.tsx` is in `runActionAllowlist`, list refresh is the confirmation). Delete modal copy renders "remove 0 device s from the group".

#### #5489 — BLOCKED
BLOCKED: needs `TICKET_MAILBOX_M365_CLIENT_ID`/`_SECRET` (empty on this stack → `POST` connect answers 400 "M365 ticket mailbox app is not configured") plus a live M365 tenant with delegated Directory.Read.All admin-consented and a consenting admin whose ID token lacks `wids`.
✅ Code at cut (`routes/tickets/mailboxConnect.ts:486`): wids fast path unchanged; on miss → `checkMailboxConsentAdminRoleViaGraph(accessToken, {tid, oid})` bound to the ID token's principal; any failure keeps the same user outcome `?ticketMailbox=insufficient_role` (only server log + Sentry for non-`no_accepted_role` reasons). Only UI-observable change: the authorize URL now requests `scope=openid profile Directory.Read.All`. `mailboxConnect.test.ts` + `microsoftIdentity.test.ts` 126/126 at the cut.

#### #6673 — PASS
API-only wave (no create UI); exercised via `fetchWithAuth` from the logged-in partner session. ✅ `POST /reports {ownerScope:'partner', type:'ticket_sla_attainment', schedule:'weekly'}` → 201, row `org_id NULL`, `partner_id` = caller's partner, `execution_scope_kind='partner_wide'`. ✅ `ownerScope:'partner'` + `device_inventory` → 400 `unsupported_report_scope`. ✅ `POST /reports/:id/generate` on it → 400 `unsupported_report_scope` (expected until W02). ✅ `PUT /reports/:id {ownerScope:'organization'}` → 400 (create-only, `expected never`). ✅ `GET /reports` all-orgs lists it; with `?orgId=` it is excluded (by design). ✅ `DELETE` → 200. ✅ Org regression: `POST /reports` org `device_inventory` → 201 (`unrestricted`), generate → 200 `completed`, run listed under `/reports/runs`.
⚠️ Web Reports ▸ Saved Reports in "All organizations" shows the partner-owned row with the raw key `reports.reportsList.reportTypes.ticket_sla_attainment`, and "Generate now" toasts the raw code `unsupported_report_scope` (web half is W02+; worth a label + friendly message before partners can create these).

#### #5856 — FAIL (portal category missing; web/PDF/public PASS)
Setup (API as admin): partner ticket categories "QA Hardware"/"QA Network"; tickets TTX-JVOVKW (Hardware) and OZU88XUGXQ (Network); billable time 1.5 h @ $120 on TTX, 2 h @ $150 on OZU, 0.5 h no-rate on TTX; part "65W PSU" $49.99 on TTX; `POST /orgs/:org/invoices/assemble` → draft 9ba0a060 (lines stored in order TTX time, OZU time, TTX part); manual line "QA manual line" $10; issued → INV-2026-0001.
✅ Web ▸ invoice ▸ Preview: "Ticket #TTX-JVOVKW: QA5856 Replace laptop PSU" + "QA Hardware" badge over Diagnose PSU + 65W PSU (part regrouped under its ticket), then "Ticket #OZU88XUGXQ: QA5856 Switch config" + "QA Network". Editor tab shows per-line ticket + category badges.
✅ PDF (`GET /invoices/:id/pdf` → 200 application/pdf, `pdftotext`): same two groups with "Category: QA Hardware" / "Category: QA Network".
✅ Public link (`/portal/invoice/<token>`, API `/invoices/public/<token>`): groups + category shown.
❌ BUG — authenticated portal (`/portal/invoices/9ba0a060…`, portal@breeze.local): groups by ticket but **no category**. `GET /api/v1/portal/invoices/9ba0a060…` → 200, every line `"ticketCategory": null`. Cause (inferred from DB, not traced in code): `getCustomerInvoice` left-joins `ticket_categories`, whose only policy is `breeze_has_partner_access(partner_id)`; the portal's org-scoped context can't see partner-owned categories, so the join yields NULL (`tickets.category` legacy text is NULL). The public route runs outside that context and gets the name. Unit tests mock the DB, so they can't catch it. Fix idea: resolve the category name via the ticket's org→partner under a partner-axis read (`readWithPartnerAxisVisibility`) or add a SELECT branch for categories of the caller org's partner.
⚠️ Web preview, PDF and both portal views put the ticket-less manual line straight after the last ticket group with no header or divider, so "QA manual line $10" reads as billed on ticket OZU88XUGXQ (medium, customer-facing). ⚠️ The authenticated portal repeats "Ticket #X" under every line in addition to the group header.

#### #6686 — PASS
✅ MSA-3: seeded quarantined `security_threats` row 43de77f5 (QA6686.EICAR.Test on e2e-windows.local); IOC Scans ▸ Threats: focus name button, Shift+Tab → focus lands on `threat-row-select-…`, Space → checkbox checked, "1 selected" bulk bar, detail NOT opened. Click threat name → detail (file path, provider, status Quarantined, timeline) with Quarantine/Restore/Remove; Restore → toast "Threat restored", row `status=allowed`, pending `security_threat_restore` command.
✅ STP-5: Settings ▸ Partner ▸ Security: "Add my current IP" + Save → PATCH 200, toast "Partner settings saved", DB `ipAllowlist:["::ffff:172.31.2.10"]`; reload, empty textarea + Save → PATCH body `security.ipAllowlist: []`, toast, DB `ipAllowlist: []`.
✅ BQ-4: Ticket TTX-JVOVKW ▸ Time & Billing: "1 entry has no hourly rate — not counted in the amount above" renders after (y=529) "Time amount $180.00" (y=509).
✅ DBT-8: DR ▸ Create Plan ▸ Save plan with empty name → "Plan name is required."; at a 1200×220 viewport, scrolled the dialog to the bottom (scrollTop 46.5) and re-clicked Save three times — each repeat save scrolled back to the error (scrollTop 23).
✅ MSA-1: Alerts ▸ Monitors ▸ New ▸ kind network_check: target defaults `8.8.8.8` (icmp_ping); Check type → HTTP check → target becomes `https://example.com`; a custom target is kept when switching to TCP port.
⚠️ (pre-existing, since 44d25e86ea) IP Allowlist textarea: pressing Enter at the end of a line does nothing — `onChange` re-parses through `parseAllowlistInput` (drops empty lines) on every keystroke, so a second entry can only be pasted; typing "10.1.1.1⏎10.2.2.2" yields `10.1.1.110.2.2.2`. The lockout confirm fired on exactly that concatenated value.
⚠️ Threat detail: "Quarantine" stays enabled on an already-quarantined threat.

### Final sweep summary

| | PASS | PARTIAL | FAIL | BLOCKED | N/A |
|---|---|---|---|---|---|
| Final sweep (12 rows) | 7 | 2 | 1 | 1 | 1 |

- **Regression caught and fixed before the tag:** #6566 made every agent disconnect publish `device.offline` twice (webhooks/automations double-fire) — #6722, merged before tagging v0.116.0.
- **Known gap shipped:** #5856 portal invoice view omits the ticket category (org-scoped portal can't read partner-axis `ticket_categories`); web, PDF and public link are correct. Tracked on #6674.
- **Still needs hardware:** #6705 on a Windows host (classifier unit tests pass); #5489 needs a live M365 tenant; #6704/#6706 need a connected DNS provider (unit suites pass).

---

## Delta sweep @ tag commit `039e0afabd` (2026-09-23)

The merge queue landed 10 PRs between the final-sweep cut (`d815846588`) and the regression fix #6722. A stable tag must be reachable from `main`, so v0.116.0 is tagged at `039e0afabd`; this section covers those 10.

| PR | Title | Surface | Status |
|---|---|---|---|
| #6714 | Upgrade preflight — breaking-change manifest, boot report, version history | api/web | PASS |
| #6702 #6708 | Metric anomaly episodes W01c (baseline anti-contamination) + W01b (assembly, auto-resolve, stage wiring) | worker/web | PARTIAL |
| #6711 | Reopen resolved tickets on inbound reply | api | PASS |
| #6715 | Emit ticket.resolved/reopened ML feedback on status change | api | PASS |
| #6703 #6709 | M365 mailbox: full text from HTML bodies; import attachments | api (M365) | BLOCKED |
| #6713 | Agent reliability memory/disk signals anchored to hardware phrasing | agent | PARTIAL |
| #6717 | replayMigration tracks constraint rewrites | test | N/A |

### Delta sweep log

#### #6714 — PASS
✅ Boot 1 (fresh DB) logged the broad report (no history -> 1 upcoming retirement `ticket-labour-pricing-fields`), then `Recorded first boot of version 0.82.0` (lab APP_VERSION is 0.82.0). `docker restart` API -> healthy; boot 2 logged `last version this deployment recorded: 0.82.0`, `Migrations: 883 applied, 0 pending`; `breeze_version_history` still exactly 1 row (ON CONFLICT DO NOTHING).
✅ Cannot block boot (code read, `databaseStartup.ts:108-130` + `upgradePreflightRunner.ts`): preflight wrapped in try/catch -> warn "continuing boot"; DB reads are best-effort -> "missing" state, capped at a 15 s timeout (worst-case added boot latency); malformed bundled manifest becomes `manifestError`, never a module-load throw; `recordRunningVersion` runs only after migrations and its throw is caught -> warn; non-release APP_VERSION skips recording. From 0.115.0 with an existing DB: table absent -> broad report, migration creates it, record inserts. Only `--strict` CLI exits non-zero, and boot never uses it.
✅ Unit: src/upgrade + databaseStartup.test.ts 5 files / 53 tests pass. No web UI in this PR (file list has no apps/web) — report is log/CLI only. CLI not runnable in the dev container (scripts/ not mounted); PR body shows tsup-built CLI verified.

#### #6702 #6708 — PARTIAL
✅ `ml.anomalies.enabled` is not in the default-on list of `defaultMlFeatureFlagValue` -> off by default; episode-resolve runs regardless by design (spec D4). API logs: migration `2026-10-28-100000-metric-anomaly-episodes.sql` applied, `[MetricAnomaliesWorker] initialized`, zero anomaly error lines across two boots. `metric_anomaly_episodes` = 0 rows (no devices/rollups in lab). No web UI exists for episodes in W01 (grep apps/web: none). Unit: 7 files / 105 tests pass.
BLOCKED (live assembly/auto-resolve): needs devices with metric rollups + flag on — not seeded (time-box).

#### #6711 — PASS
✅ Seeded open email ticket T-2026-6711 (thread key + portal requester jane-delta@known.test). Resolved in UI (status select -> Resolution note -> Resolve ticket; "Ticket resolved" toast; feed "Breeze Admin changed status: Open to Resolved"). Ran `processInboundEmail` in the API container (same entry the Mailgun webhook worker calls; webhook itself needs MAILGUN_INBOUND_SIGNING_KEY, unset in lab) with In-Reply-To = thread key. First try: `ignored / inbound disabled for partner` (correct gate); after enabling `settings.ticketing.inbound.enabled`: `ticket_email_inbound.parse_status=matched`, ticket `open`, `resolved_at` NULL; comments = public email comment + `status_change` resolved->open (author "Inbound Email", user_id NULL, internal); `ticket_outbox` has `ticket.status_changed {from:resolved,to:open}`. Unit (ticketService + inboundEmailService in the combined run) green.

#### #6715 — PASS
✅ `ml_feedback_events` for the same ticket: `ticket.resolved` (actor = admin user, metadata fromStatus open/toStatus resolved/minutesOpen 0) on UI resolve; `ticket.reopened` (actor NULL, resolved->open, minutesOpen 1) on the inbound reopen — exactly one each. ⚠️ DLT-1: the reopen row says `source: "manual_update"` for an inbound-email reopen.

#### #6703 #6709 — BLOCKED
BLOCKED: needs a connected M365 tenant mailbox (Graph) to ingest HTML-body mail with attachments. Unit: inboundAttachments, inboundEmailWorker, fetchInboundAttachments, graphMailClient, normalizeGraphMessage, inboundEmailService — 6 files / 142 tests pass.

#### #6713 — PARTIAL
✅ `go test -v ./internal/collectors/ -run '^(<all Test funcs in reliability_test.go>)$'` -> 82 PASS / 0 FAIL (incl. new `TestClassifyHardwareTypeMessageSignals`); full `go test ./internal/collectors/` ok. BLOCKED live: needs a Windows agent with real System-log hardware events.


#### Windows lab — #6705 / #6713 (orchestrator)
- `go test ./internal/collectors/ -count=1` from the tag commit, run natively on the Windows Server 2022 lab VM (Go 1.26.6): `ok … 15.0s` — covers the build-tagged `eventlogs_windows.go` path plus the classifier and reliability suites. Live event-log collection by an enrolled agent was not exercised → noted as a known gap in the release body.

### Delta sweep summary
6 rows: 3 PASS · 2 PARTIAL · 1 BLOCKED (M365 tenant) · 0 FAIL. Nothing blocks the tag. v0.116.0 is tagged at `039e0afabd`.
