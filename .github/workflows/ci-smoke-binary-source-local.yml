name: smoke-binary-source-local

# Verifies that the BINARY_SOURCE=local agent-update path stays end-to-end
# functional. v0.65.8 (#625) shipped a strict-signing change that broke this
# silently — the regression only surfaced when an agent tried to update.
# This job makes that scenario fail the PR instead of the fleet.

on:
  pull_request:
    paths:
      - "apps/api/src/services/binarySync.ts"
      - "apps/api/src/services/binarySync.selftest.ts"
      - "apps/api/src/services/manifestSigning.ts"
      - "apps/api/src/routes/agentVersions.ts"
      - "apps/api/src/db/schema/manifestSigningKeys.ts"
      - "apps/api/migrations/**"
      - ".github/workflows/ci-smoke-binary-source-local.yml"
  push:
    branches: [main]

env:
  PNPM_VERSION: '10.34.5'

permissions:
  contents: read

jobs:
  smoke:
    name: BINARY_SOURCE=local download path
    runs-on: ${{ fromJSON((github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository) && '["ubuntu-24.04"]' || '["self-hosted","linux","cloudcom"]') }}
    timeout-minutes: 10

    services:
      # pgvector-enabled image, though this job no longer NEEDS it: built-in
      # extensions load only when their enable flag is set, and this job
      # leaves BREEZE_WORKSPACE_ENABLED unset, so ee/workspace/migrations/
      # *.sql (CREATE EXTENSION vector) never runs. Kept to match the image
      # .env.example ships, so the boot proved here is the one self-hosters
      # run — a stock postgres:16-alpine would boot identically.
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: breeze
          POSTGRES_PASSWORD: breeze
          POSTGRES_DB: breeze
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U breeze"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          version: ${{ env.PNPM_VERSION }}
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version-file: .node-version
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build API
        run: pnpm --filter=@breeze/api build

      # NOTE: no `ee/workspace` web build here, deliberately. Built-in
      # extensions are OFF unless the deployment sets their enable flag, and
      # this job leaves BREEZE_WORKSPACE_ENABLED unset — so it proves the
      # DEFAULT-OFF boot from a plain checkout: no built-in migrations, no
      # dist/web requirement (which is what used to force a build:web step
      # here), and no pgvector dependency. Flag-ON coverage lives in the
      # integration shard-1 boot step in ci.yml.

      - name: Stage a fake local agent binary
        env:
          WORKSPACE: ${{ github.workspace }}
        run: |
          mkdir -p "$WORKSPACE/tmp/agent/bin"
          dd if=/dev/urandom of="$WORKSPACE/tmp/agent/bin/breeze-agent-linux-amd64" bs=1024 count=4
          echo "0.65.9" > "$WORKSPACE/tmp/VERSION"

      - name: Boot API in BINARY_SOURCE=local mode
        env:
          WORKSPACE: ${{ github.workspace }}
          BINARY_SOURCE: local
          BREEZE_VERSION: "0.65.9"
          DATABASE_URL: postgres://breeze:breeze@localhost:5432/breeze
          DATABASE_URL_APP: postgres://breeze_app:breeze@localhost:5432/breeze
          BREEZE_APP_DB_PASSWORD: breeze
          POSTGRES_PASSWORD: breeze
          REDIS_URL: redis://localhost:6379
          # The CI Redis service has no password; #909 made unauthenticated
          # Redis fail-closed in self-hosted production (IS_HOSTED=false
          # below). This override acknowledges the CI-only sandbox posture
          # so resolveRedisUrl() warns instead of throwing. Without it the
          # smoke step exits 1 at boot before the binary-download path is
          # ever exercised. See #969.
          BREEZE_ALLOW_UNAUTH_REDIS: "true"
          NODE_ENV: production
          PUBLIC_APP_URL: http://localhost:3001
          # Production-strict env that the API requires at boot post-#568.
          JWT_SECRET: ci-smoke-bsl-k7x9Qm4pR2vL8nW5jT3yF6hB0cA1dE4gI7
          # Partner reconstruction API cursor signing key (32 random bytes,
          # canonical base64) — required at production boot post-#2506.
          PARTNER_API_CURSOR_SIGNING_KEY: fD9k9y5kfWTroMAjLM4+tpKrbOHrbu+4Bq775lViAHw=
          APP_ENCRYPTION_KEY: e9367714341ea19491ff7cd095d1a97e15ae3f7962416723ecfb29a89ed71eb9
          MFA_ENCRYPTION_KEY: e86f8b88f2c058d428de55c4abec1b50a3cd960a7205dffdb65be8130abbb777
          ENROLLMENT_KEY_PEPPER: ci-smoke-bsl-enrollment-pepper-Hy4Bz9Mp2Wq6Tv0Lk3
          MFA_RECOVERY_CODE_PEPPER: ci-smoke-bsl-mfa-recovery-pepper-Nq8Zx2Vm5Tr9Lp1
          AGENT_ENROLLMENT_SECRET: ci-smoke-bsl-agent-enrollment-Hy4Bz9Mp2Wq6Tv0Lk3
          ENROLLMENT_SECRET_ENFORCEMENT_MODE: warn
          IS_HOSTED: "false"
          # Required in production by the Wave 3 live-authorization epoch
          # rollout — compat until the fleet drains version-one writers.
          EVENT_PERMISSION_EPOCH_MODE: compat
          # Wave 4 remote-access runtime config (#2842) — required at
          # production boot by getRemoteWsRuntimeConfig(). Values match the
          # CI stack in ci.yml; irrelevant to the binary-download path this
          # smoke exercises, but the API refuses to start without them.
          REMOTE_ACCESS_ADMISSION_MODE: open
          REMOTE_WS_AUTH_MODE: post_upgrade
          REMOTE_WS_REDIS_TOPOLOGY: standalone-single-primary
          TRUSTED_PROXY_CIDRS: 127.0.0.1/32
          CORS_ALLOWED_ORIGINS: http://localhost:3001
          TRUST_PROXY_HEADERS: "false"
          # First-boot admin bootstrap: required by production mode when users
          # table is empty. The smoke job doesn't auth as a user; these just
          # let the API finish startup.
          BREEZE_BOOTSTRAP_ADMIN_EMAIL: smoke-admin@example.test
          BREEZE_BOOTSTRAP_ADMIN_PASSWORD: ci-smoke-bsl-bootstrap-admin-Hy4Bz9Mp2Wq6Tv0Lk3
          # Production validator now requires the manifest trust root for
          # both BINARY_SOURCE=github AND BINARY_SOURCE=local (Task 27 fix).
          # In local mode the API still generates its own per-deployment key
          # for signing manifests, but the trust root is the verification
          # anchor; without it, agents would accept unsigned manifests. We
          # use the public-key fixture from the github-source smoke job.
          RELEASE_ARTIFACT_MANIFEST_PUBLIC_KEYS: "yzx8ftmcls6uBetFC5SYnZhBo+cbur3IX50TbBthTso="
        run: |
          export AGENT_BINARY_DIR="$WORKSPACE/tmp/agent/bin"
          export BINARY_VERSION_FILE="$WORKSPACE/tmp/VERSION"
          pnpm --filter=@breeze/api start &
          API_PID=$!
          echo "Waiting for /health/ready..."
          for i in $(seq 1 60); do
            if curl -sf http://localhost:3001/health/ready >/dev/null 2>&1; then
              echo "API healthy after ${i} polls."
              break
            fi
            if ! kill -0 "$API_PID" 2>/dev/null; then
              echo "API process exited before becoming healthy — boot self-test likely failed."
              exit 1
            fi
            sleep 2
          done
          curl -sf http://localhost:3001/health/ready || { echo "API never became healthy"; kill "$API_PID" 2>/dev/null; exit 1; }
          echo "API_PID=$API_PID" >> "$GITHUB_ENV"

      - name: Verify download endpoint returns 200 with signed manifest
        run: |
          STATUS=$(curl -s -o /tmp/dl.json -w '%{http_code}' \
            'http://localhost:3001/api/v1/agent-versions/0.65.9/download?platform=linux&arch=amd64&component=agent')
          echo "Download endpoint returned: ${STATUS}"
          cat /tmp/dl.json | jq . || cat /tmp/dl.json
          if [ "$STATUS" != "200" ]; then
            echo "Expected 200 (regression: #625 returned 409 here on v0.65.8)."
            exit 1
          fi
          jq -e '.manifest != null and (.manifest | length) > 0' /tmp/dl.json
          jq -e '.manifestSignature != null and (.manifestSignature | length) > 0' /tmp/dl.json
          jq -e '.signingKeyId != null and (.signingKeyId | startswith("deploy-"))' /tmp/dl.json

      - name: Stop API
        if: always()
        run: |
          if [ -n "${API_PID:-}" ]; then
            kill "$API_PID" 2>/dev/null || true
          fi
